const mongoose = require('mongoose');

const promptSchema = new mongoose.Schema({
  prompt: {
    type: String,
    required: true,
  },
  enhancedPrompt: {
    type: String,
  },
  aiResponse: {
    type: String, // The story or explanation generated by Gemini
  },
  negativePrompt: {
    type: String,
    default: '',
  },
  style: {
    type: String,
    default: 'Realistic',
  },
  imageUrl: {
    type: String,
    required: true,
  },
  user: {
    type: mongoose.Schema.Types.ObjectId,
    ref: 'User',
    required: true,
  },
  createdAt: {
    type: Date,
    default: Date.now,
  },
});

module.exports = mongoose.model('Prompt', promptSchema);
